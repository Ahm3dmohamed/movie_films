import 'package:flutter/material.dart';

class MoveiTab extends StatelessWidget {
  const MoveiTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.brown,
    );
  }
}
